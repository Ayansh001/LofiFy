# Enable legacy peer dependencies to avoid compatibility issues
legacy-peer-deps=true

# Set registry to npm
registry=https://registry.npmjs.org/

# Increase network timeout for larger dependencies
network-timeout=60000
 What does that mena